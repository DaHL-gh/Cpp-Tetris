{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Debug",
            "program": "${workspaceFolder}\\test.exe",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: clang++.exe build active file"
        }
    ]
}